import { describe, it, expect } from "vitest";
import * as exports from "notes-service-common";

describe("notes-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
